<br />

<div id="pageCanvas">
  <div id="pageWrap">
    <% @page.id %>
    <% @pages.length %>
    <%= @elements.length %>
    <ul id="main_page_nav">
      <% @pages.each do |page| %>
        <li><%= link_to(page.title, edit_webpage_path(page)) %></li>
      <% end %>
    </ul>
  <div>
</div>